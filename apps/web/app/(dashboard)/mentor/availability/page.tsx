import { EmptyState } from "@/components/ui/custom/empty-state";

export default function MentorAvailabilityPage() {
  return (
    <EmptyState
      eyebrow="Mentor"
      title="Availability"
      description="Mentors will define bookable time slots and session durations here."
    />
  );
}
